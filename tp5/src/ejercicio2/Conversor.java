/* public class Conversor {
    // Constantes estáticas para tasas de conversión
    private static final double CENTIMETROS_POR_PULGADA = 2.54;
    private static final double LIBRAS_POR_KILOGRAMO = 0.453592;
    private static final double GALONES_POR_LITRO = 3.78541;

    // Método estático para convertir centímetros a pulgadas
    public static double centimetrosAPulgadas(double centimetros) {
        return centimetros / CENTIMETROS_POR_PULGADA;
    }

    // Método estático para convertir pulgadas a centímetros
    public static double pulgadasACentimetros(double pulgadas) {
        return pulgadas * CENTIMETROS_POR_PULGADA;
    }

    // Método estático para convertir libras a kilogramos
    public static double librasAKilogramos(double libras) {
        return libras * LIBRAS_POR_KILOGRAMO;
    }

    // Método estático para convertir kilogramos a libras
    public static double kilogramosALibras(double kilogramos) {
        return kilogramos / LIBRAS_POR_KILOGRAMO;
    }

    // Método estático para convertir galones a litros
    public static double galonesALitros(double galones) {
        return galones * GALONES_POR_LITRO;
    }

    // Método estático para convertir litros a galones
    public static double litrosAGalones(double litros) {
        return litros / GALONES_POR_LITRO;
    }


    public static void main(String[] args) {
         // Convertir centímetros a pulgadas
         double centimetros = 10.0;
         double pulgadas = Conversor.centimetrosAPulgadas(centimetros);
         System.out.println(centimetros + " centímetros son " + pulgadas + " pulgadas.");
 
         // Convertir pulgadas a centímetros
         pulgadas = 5.0;
         centimetros = Conversor.pulgadasACentimetros(pulgadas);
         System.out.println(pulgadas + " pulgadas son " + centimetros + " centímetros.");
 
         // Convertir libras a kilogramos
         double libras = 10.0;
         double kilogramos = Conversor.librasAKilogramos(libras);
         System.out.println(libras + " libras son " + kilogramos + " kilogramos.");
 
         // Convertir kilogramos a libras
         kilogramos = 5.0;
         libras = Conversor.kilogramosALibras(kilogramos);
         System.out.println(kilogramos + " kilogramos son " + libras + " libras.");
 
         // Convertir galones a litros
         double galones = 2.0;
         double litros = Conversor.galonesALitros(galones);
         System.out.println(galones + " galones son " + litros + " litros.");
 
         // Convertir litros a galones
         litros = 10.0;
         galones = Conversor.litrosAGalones(litros);
         System.out.println(litros + " litros son " + galones + " galones.");
     }
}*/

